node_modules/
dist/
.env
.env.local
.dev.vars
.wrangler/
*.local
.DS_Store
# bun.lock is committed — it pins every dependency (supply-chain protection).
# package-lock.json stays ignored: bun is the package manager here; a stray
# `npm install` shouldn't introduce a second, competing lockfile.
package-lock.json
work.md
verify-admin-ui.mjs
graphify-out/