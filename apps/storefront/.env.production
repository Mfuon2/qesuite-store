# ─── apps/storefront deployed at store.qesuite.com ────────────────────────
# Serves: customer-facing storefront PWA

# Worker API URL — update after `wrangler deploy`
# NOTE: /api suffix is required (storefront calls /api/storefront/... directly)
VITE_API_URL=https://qesuite-worker-api.leemfo.workers.dev/api
